commands:
  install: cargo fetch && cargo build
  typecheck: cargo check
  build: cargo build --release
  test: cargo test -- --test-threads=4
  lint: cargo clippy -- -D warnings
  clean: cargo clean

services: {}
